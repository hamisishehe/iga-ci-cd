# ===============================
# =   Application Configuration =
# ===============================
spring.application.name=iga_veta
server.port=8080

# ===============================
# =   Datasource Configuration  =
# ===============================
# Use environment variables for secure credentials
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# =   JPA / Hibernate Config    =
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.time_zone=Africa/Nairobi
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# =   Hikari Connection Pool    =
# ===============================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.pool-name=HikariCP
spring.datasource.hikari.initialization-fail-timeout=60000


# ===============================
# =   JWT Configuration         =
# ===============================
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}

# ===============================
# =   Optional Dev Settings     =
# ===============================
# For local development, you can set a default fallback (optional)
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/iga-database
# SPRING_DATASOURCE_USERNAME=postgres
# SPRING_DATASOURCE_PASSWORD=Hamisi2233.
# JWT_SECRET=secret
# JWT_EXPIRATION=86400000
